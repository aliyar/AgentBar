# generated by xcodegen
AgentBar.xcodeproj/
Supporting/Info.plist
Supporting/AgentBar.entitlements
Supporting/AgentBarWidget-Info.plist
Supporting/AgentBarWidget.entitlements

# build output
build/
DerivedData/
.build/
dist/
*.xcarchive
*.dmg
*.zip
# except the release zips the site serves when no download bucket is set (scripts/release.sh)
!site/public/releases/*.zip

# xcode / spm user state
xcuserdata/
*.xcuserstate
*.xcscmblueprint
.swiftpm/
Package.resolved

# macOS
.DS_Store
*.profraw

# release credentials: the name of this machine's notarytool keychain profile
.notary-profile

# landing page (Next.js static export)
site/node_modules/
site/.next/
site/out/
site/next-env.d.ts
site/*.tsbuildinfo

# AI assistant / editor metadata - never published
.claude/settings.local.json
CLAUDE.md
CLAUDE.local.md
**/CLAUDE.md
.cursor/
.cursorrules
.aider*
.copilot/
*.plan.md
.vscode/

# impeccable working files (the design skill): questions, review captures, mocks
site/.impeccable/questions/
site/.impeccable/review/
site/.impeccable/mocks/
