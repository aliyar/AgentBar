# generated by xcodegen
AgentBar.xcodeproj/
Supporting/Info.plist
Supporting/AgentBar.entitlements
Supporting/AgentBarWidget-Info.plist
Supporting/AgentBarWidget.entitlements

# build output
build/
DerivedData/
.build/
dist/
*.xcarchive
*.dmg
*.zip

# xcode / spm user state
xcuserdata/
*.xcuserstate
*.xcscmblueprint
.swiftpm/
Package.resolved

# macOS
.DS_Store
*.profraw

# release credentials: the name of this machine's notarytool keychain profile
.notary-profile

# landing page (Next.js static export)
site/node_modules/
site/.next/
site/out/
site/next-env.d.ts
site/*.tsbuildinfo

# AI assistant / editor metadata - never published
.claude/
CLAUDE.md
CLAUDE.local.md
**/CLAUDE.md
.cursor/
.cursorrules
.aider*
.copilot/
*.plan.md
.vscode/

# The site's own briefs and design working files: they live in the notes repository,
# not here, so the code repository holds only what could be public.
site/PRODUCT.md
site/DESIGN.md
site/.impeccable/
